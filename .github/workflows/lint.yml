name: Lint

on: [push, pull_request]

jobs:
  lint:
    name: Checking for code conformity with Black and isort
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-python@v2
      - name: Lint isort
        run: isort --check --diff Pyrado
      - name: Lint black
        uses: psf/black@stable
        with:
          args: "--check Pyrado"
